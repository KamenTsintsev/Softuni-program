function characterSequence(wordInput) {
    let word = wordInput;

    for (let i = 0; i < word.length; i++) {
        let letter = word[i];
        console.log(letter);
    }
}

characterSequence("softuni");